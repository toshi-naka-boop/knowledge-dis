タスク名: knowledge-discovery
格納先: design/knowledge-discovery/

# spec-input: ナレッジ・ディスカバリ（All Things Agentic Hackathon提出用）

作成日: 2026-08-17
提出先: All Things Agentic Hackathon（Devpost / Google）
トラック: The Fortified Enterprise Fleet
締切: 2026-08-31 17:00 PDT（= 9/1 09:00 JST）
提出物: デモ動画 / コードリポジトリ / アーキテクチャ図 / 短いwrite-up（英語）
技術要件: Gemini + Google Cloud上で動作すること

---

## 目的

社員一人一人に付く個人AIエージェント群が「これって社内の誰に聞けばいい？」を解決するマルチエージェントシステムのデモを完成させる。
設計思想: **情報だけならAI同士で完結させ、人間が登場するのは人間同士のつながり自体が価値のとき。** 全エージェント間通信は本人同意ゲート付きで監査可能。

一言説明（提出済みの登録文言）:
> A multi-agent knowledge-discovery system where every employee's personal agent answers "who should I ask about this?" — agents resolve information requests among themselves, and involve humans only when the connection itself is the value. All inter-agent disclosure is owner-approved, consent-gated, and auditable.

## スコープ（2週間で作るもの）

1. 模擬社員4名ぶんの個人エージェント（ADKマルチエージェント構成）
2. 承認済みプロフィール: エージェントがシードデータ（模擬の職務文書）から下書き→**業務由来項目はデフォルト公開**とし、本人は一度のレビューで修正・NDA/秘匿項目の非公開化のみ行う（全項目承認待ちにはしない）
3. レーン1（情報レーン）: 質問→ディスカバリ層がプロフィール索引で上位k体に配送→受信側エージェントが承認済みナレッジの範囲のみで回答/リンク返却→人間は呼ばれない
4. レーン2（接続レーン）: エージェント間で解決不能な案件のみ本人へ「会いますか？」→承諾時のみマッチ成立→15分MTG提案。辞退は依頼者に不可視
5. 公開範囲の最小版: プロフィール項目ごとに 公開/非公開 のトグル（NDA対応の思想を示す最小実装）
6. 監査ダッシュボード＝**エージェントチャンネル可視化画面**: 本来非公開のエージェント間会話をデモ用に観客に見せる画面で、全メッセージのログ表示（誰が・何を・同意状態つき）と兼用。悪性クエリの拒否もこの画面に赤表示で流す。非エンジニアが5秒で異常に気づける画面を目標
7. デモ動画（3分・英語）: レーン1完結→レーン2の同意と不可視辞退→監査画面、の1本道
8. アーキテクチャ図とwrite-up（英語）

## 非スコープ（明示的に作らない）

- 実GWSデータ連携（カレンダー/Gmail/議事録の取り込み）— シードデータで代替
- 1on1支援機能全般（Zenn vol.5側の別プロダクト）
- 被マッチクォータ、workload連動の応答提案
- 貢献の可視化（余裕があれば簡易カウンタのみ、stretch扱い）
- 組織集計・属人化リスク検出
- 本番想定の認証・マルチテナント

## 決定事項

1. トラックはFortified Enterprise Fleetとする。
2. 課題設定は2レーン構造とする: 情報照会はエージェント間で完結（レーン1）、人間が出るのは接続価値がある場合のみで形式はMTG一択（レーン2）。
3. テキスト流通の原則: 人間が書いた、または機械が下書きし本人が承認したテキストのみ流通可。未承認の機械生成テキストは型付きフィールドのみ。
4. 受信側エージェントが代理回答に使えるのは、本人が公開を承認したプロフィール・ナレッジのみ（私的データからは答えない）。
5. 辞退は依頼者に通知しない（断るコストゼロ。これが唯一のハブ保護）。
6. エージェント間通信は単一のメッセージエンベロープ（from/to/intent/payload_type/payload/consent/audit_id）に統一し、全件監査ログへ記録する。
7. 流通可能なpayload型はスキーマレジストリで中央定義し、未登録型は送信層で拒否する（出口の一本化）。
8. 配送は全体ブロードキャストせず、プロフィール索引による上位k体のみ。
9. 実装はADK（Python）+ Gemini（Flash優先、複雑な推論のみPro）+ Cloud Run + Firestoreを基本構成とする。
10. デモの舞台は作者が実感を持って語れる業務設定とし、知識の浅い現場の描写はしない（具体は未決事項）。
11. write-upでは既存カテゴリ（Starmind/Glean/Viva Topics）へのアンチテーゼとして位置づけ、Viva Topics撤退（2025/2）とCSCW研究の失敗要因（選定の社会的配慮・報酬設計・参加コスト）への設計回答であることを明記する。
12. デモ動画は3分・英語・1シナリオ一本道とし、機能羅列をしない。デモ構成は幸福経路2本（レーン1完結／レーン2同意）＋防御1本（悪性クエリ拒否）とする。
13. プロフィール索引にはGemini合成の模擬社員プロフィール約400名分を実データとしてFirestoreに投入する（完全実装エージェントは4体のまま。モック画面による見せかけは行わない）。
14. 本人レビューの位置づけは「所有権の主張」ではなく、①代理発言の検品（幻覚プロフィールによる誤配送防止）②定着装置（CSCW文献の失敗要因への回答）とする。write-upでも所有権を根拠にしない（仕事内容は日米とも法的には会社帰属であるため）。
15. 用語の統一: プロフィールの本人確認は「レビュー（検品）」、レーン2の応諾は「同意（consent）」、公開/非公開設定は「公開範囲（visibility）」と呼び分け、「承認（approval）」という語は使わない。英語表記は owner-reviewed / consent-gated / visibility。spec内の既存の「承認」表記はこの決定に従い読み替え、実装・write-upではレビュー系の語に統一する。

## 検討済み・却下

- エージェント間を全てenum/boolに縛る案 — 却下（マッチ精度が出ず過剰。本人承認テキストの流通で置換）
- 被マッチ月間クォータ＋2番手ルーティング — 却下（同意＋辞退不可視で保護は足りる。頻繁に呼ばれる人は重要人物であり制限対象ではない）
- クエリへのpurpose enum（情報取得/繋がりたい）付与 — 却下（レーンは本人の選択肢ではなくシステムの構造で決まる）
- 非同期テキスト中継レーン — 却下（情報ならレーン1、人が出るならMTG。中間は作らない）
- 属人化リスクの経営向け検出 — スコープ外へ（監視ツール化の懸念。本人のためのエージェントという信頼設計と衝突）
- 1on1支援との同時提出 — 却下（スコープ膨張の反省。Zenn vol.5へ分離）

## 前提

- 開発者は1名、フルタイム勤務の業務外時間のみ。実働は平日夜＋土日2回分程度。
- Google Cloud $150クレジット申請済み（承認待ちの可能性あり。個人/Momonga側アカウント使用）。未承認のまま開発期限が来た場合は個人課金＋予算アラートで進める（Flash中心なら実費は小額の見込み）。
- GEAP（Gemini Enterprise Agent Platform）はトラック推奨技術だが、習熟コスト次第で採用範囲を判断する（下記未決事項）。
- Devpostユーザー名: nejimakitoshi。ハッカソン参加登録済み。
- 8/20からZenn vol.5エントリーが並走する。8/31まではknowledge-discoveryを優先する。

## 未決事項

- **【design-loop初期論点】レーン1の回答方式**: 決定3（未レビューの機械生成テキストは流通不可）と、レーン1の「受信側エージェントが回答を生成して返す」動作が矛盾している。生成回答は本人がレビューしていない機械生成テキストであり、幻覚や承認外情報（暗黙の抜け道等）が本人名義で流れるリスクがある。選択肢: (a) 引用＋リンク＋型付きメタデータのみ返す（エージェントは選択のみ行い著者にならない。チャット検討時の推奨案）／(b) 生成要約を許可し「AI生成・本人未確認」フラグを型で必須化＋生成コンテキストを承認済み文書のみに物理制限。design-loopの批評工程で決定し、ledger.mdに解決理由を記録すること。

- プロダクト名（未定。write-upまでに決める）
- GEAPコンポーネントの採用範囲: Agent Registry / Model Armor / Agent Observabilityのうちどれを実採用し、どれをアーキ図上の将来構成として言及に留めるか（審査上はFortifiedトラック推奨技術のため、最低1つは実採用したいが工数と要相談）
- 本人承認UIの形態（最小Webアプリか、Slack風の模擬チャットUIか）
- 監査ダッシュボードの実装方式（専用画面か、Cloud Loggingベースの簡易ビューか）
- デモ動画のナレーション（自声英語か、TTSか）
- デモの舞台となる業種・部門設定、および模擬社員4名の人物・ナレッジ設定（作者が実体験ベースで書ける範囲から選ぶ。候補: 人材紹介会社のバックオフィス/営業/RA・CA/システム管理、あるいは一般的なコーポレート部門）

## 制約

- 締切8/31 17:00 PDT厳守。逆算で8/29までに機能凍結、残りを動画・図・write-upに充てる。
- Gemini APIとGoogle Cloudコンピュートの使用が審査条件。
- コスト: クレジット$150内。Cloud Runはmin-instances=0、デモ収録後は即停止。公開URLはAPIキー等で保護。
- 評価基準はInnovation & Operational Utility 40%（実世界の摩擦をどれだけ除去するか）が最大配点。デモは機能自慢でなく摩擦除去の実演にする。
